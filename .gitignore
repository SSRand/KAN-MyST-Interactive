.venv/
__pycache__/
*.pyc
*.pyo
.DS_Store
.ipynb_checkpoints/
_build/
node_modules/
*.log
.jupyter/

# uv.lock is intentionally NOT ignored — it pins the deploy environment.
# Application repos commit the lockfile so Docker builds and local dev share
# the exact same resolved versions.
